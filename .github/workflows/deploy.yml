name: Deploy to GitHub Pages

on:
  push:
    branches:
      - main

# Sets permissions of the GITHUB_TOKEN to allow deployment to GitHub Pages
permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '16'

      - name: Install dependencies
        run: npm install

      - name: Check for API key
        run: |
          if [ -z "${{ secrets.GOOGLE_TTS_API_KEY }}" ]; then
            echo "ERROR: GOOGLE_TTS_API_KEY secret is not set in repository settings"
            exit 1
          else
            echo "API key is set and available"
          fi

      - name: Create config file with API key directly
        run: |
          echo "// Configuration for the vocabulary learning app" > config.js
          echo "const config = {" >> config.js
          echo "    googleTTSApiKey: '${{ secrets.GOOGLE_TTS_API_KEY }}'" >> config.js
          echo "};" >> config.js
          cat config.js

      - name: Ensure API key is used in index.html
        env:
          GOOGLE_TTS_API_KEY: ${{ secrets.GOOGLE_TTS_API_KEY }}
        run: |
          node update-api-key.js

      - name: Deploy to GitHub Pages
        uses: JamesIves/github-pages-deploy-action@v4
        with:
          folder: .
          branch: gh-pages
          token: ${{ secrets.GITHUB_TOKEN }}
          clean: true 